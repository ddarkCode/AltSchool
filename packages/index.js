//Simple example to text the power of open souurce libraries

const {mean, pow, median} = require('mathjs')

console.log(mean([2, 4, 6, 8, 12, 45]));
console.log(pow(5,5));
console.log(median([22, 44, 13, 15, 4]));